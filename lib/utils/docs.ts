/**
 * The address-form page on the documentation site, where every pointer this
 * package prints leads. It used to name `/address-form`, a path the site no
 * longer serves (#4). Stated once, so the page's next move is one edit;
 * `lib/readme.test.ts` holds the code and the README to it.
 */
export const DOCS = "https://docs.chaosity.cloud/docs/client-libraries/address-form";
